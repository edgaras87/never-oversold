# Change-plan: the pre-briefing arrangement update

## Summary — the state after all commits

The kit's conventions stand at af16eb7 in the registry and in the
tree: three skill copies overwritten (compare-first found every
copy identical to c670fe5), the entry file's guard comment carrying
the stub's new wording, the hygiene base ignoring the operator's
local file. The kit's new tracked settings file with its commit ask
rule is not taken and the registry says why. The entry file sits
under `.claude/` with every other agent-side file, its content
untouched. The operator's `CLAUDE.local.md` exists at the root,
ignored, and the registry names it without quoting it. TODO's Later
item for the branch rule's trial names all three arrivals; the
devlog says what the receipt branch gave the compare and where it
fell short. Nothing names the problem.

## Commits

**1. `docs(agent): add change-plan for the arrangement update`**
This plan, agreed.

**2. `chore: ignore CLAUDE.local.md`**
Repo-hygiene's base template changed at af16eb7: one ignore line and
a firmer comment on the settings file. Project side, so it cannot
share the registration commit that follows; this is the reason the
set needs a plan.

**3. `chore(agent): update the kit's conventions @ af16eb7`**
The three changed skill copies (commit-messages, change-plans,
convention-lifecycle) overwritten from the kit; agent-arrangement's
stub comment change carried into the living entry file (tests 2 and
3 of the guard); one registry entry for the whole kit at af16eb7 —
what changed, what did not (artifact-kinds, the project-recording
stubs), the settings file rejected and why, the hygiene line landed
in commit 2.

**4. `refactor(agent): move CLAUDE.md under .claude/`**
A rename, content untouched, with its registry entry: every
agent-side file under one directory; the convention now permits the
address; the kit's default waits on this run's evidence.

**5. `docs(agent): log the operator's local file`**
Registry entry: `CLAUDE.local.md` exists, ignored, holds the
reviewer's pace. Its words enter no record.

**6. `docs: records catch up on the arrangement update`**
TODO's Later item for the branch rule's trial extended to name the
kit update, the operator's file, and the entry file's move. Devlog
entry for this session: the receipt branch, what it gave the compare
and where it fell short, the rejection, the move.

**7. `docs(agent): close change-plan for the arrangement update`**
Deletes this file; the body records what diverged.

After the close, outside the set: `temp/` deleted (untracked, never
committed), and the branch fast-forwarded into main on the
reviewer's word.

## Decisions taken inside this plan

- **The settings file is rejected, not deferred.** The reviewer
  holds the pace through the local file and the commit-messages
  rule; the handbook withdrew the same rule in its own checkout
  (ADR-0035, decision 2's note) because it asked for a word already
  given in chat. The receipt branch keeps the file so the compare
  stays faithful; the rejection lives in the registry entry.
- **Project side lands before registration** (commit 2 before 3), so
  the registry entry names a commit that exists rather than one to
  come.
- **One registry entry for the kit, not one per convention.** The
  kit moved as one commit at one hash; the entry lists which
  conventions changed. Convention-lifecycle §8 step 5 allows the
  split and never requires it.
- **The entry file's move and the operator's file get their own
  entries and commits.** Each is one arrangement decision; the log
  is one entry per decision.
- **This set runs on its own branch though the branch rule is on
  trial only from Step 1** — the reviewer's choice for this session,
  not the rule firing early.
