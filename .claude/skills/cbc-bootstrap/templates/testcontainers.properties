# Template — master copy in correctness-by-construction (CBC ADR-0008);
# checked against concept v1 (CBC ADR-0003, CBC ADR-0005 — practice-born).
# Extracted 2026-08-28 from the lived ~/.testcontainers.properties
# behind checkout-system's harness (PLAN Step 6). Changes on
# extraction: uid generalized to <uid>. Copy to $HOME as
# ~/.testcontainers.properties and fill — it binds ONLY from $HOME,
# never the project root (walkthrough trap); machine-level, once per
# machine, operator-manual territory. The filled file is the
# machine's own (CBC ADR-0008).
# Harvested 2026-09-12 from never-oversold (run 3 of the pure seed)
# Step 4, read read-only (CBC ADR-0007): the ryuk.disabled line
# dropped — Testcontainers 2.x reads no such key (checked against
# the 2.0.5 jar: the properties file reads only ryuk.container.image,
# .privileged, .timeout; the disable switch is the environment
# variable TESTCONTAINERS_RYUK_DISABLED), and Ryuk ran unmodified
# under rootless podman 5.8, reaping the throwaways within seconds.
docker.host=unix:///run/user/<uid>/podman/podman.sock
