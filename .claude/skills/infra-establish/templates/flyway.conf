# Template — master copy in correctness-by-construction (ADR-0008);
# checked against concept v1 (ADR-0003, ADR-0005 — practice-born).
# Extracted 2026-08-28 from checkout-system's lived
# infrastructure/flyway/conf/flyway.conf (PLAN Step 6). Changes on
# extraction: identities generalized to placeholders; run-local ADR
# reference generalized to neutral wording. Copy into a run at the
# same path and fill; the filled file is the run's own (ADR-0008).

# Flyway — the only DDL path, per the run's ground constraints.
# No credentials here: user/password are env-passed by compose
# (FLYWAY_USER=<project>_migrator). The url speaks the compose
# network's service name — this config is for the one-shot container.
flyway.url=jdbc:postgresql://postgres:5432/<project_db>
flyway.schemas=<project_schema>
flyway.locations=filesystem:/flyway/sql
